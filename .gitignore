# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / Editor files
.DS_Store
Thumbs.db

# Node modules (important!)
node_modules/
node_modules/*

# Next.js build output
.next/

# Build output
build/
dist/

# Environment files
.env
.env.*  # e.g. .env.local, .env.production

# Coverage directories (if you use Jest or other testing)
coverage/

# Tailwind / PostCSS
*.css.map

# Optional: lockfiles (if you prefer not to commit them)
# package-lock.json
# yarn.lock

# If using separate front/back ends, apply a similar .gitignore in each
# or structure it to cover both
